[pytest]
testpaths = tests
asyncio_mode = auto
addopts = --cov=pipeline --cov=api --cov-report=term-missing --cov-report=html:htmlcov
markers =
    integration: marks tests as system integration tests
    plaid_sandbox: marks tests requiring Plaid sandbox credentials
